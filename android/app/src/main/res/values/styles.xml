<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>

    <!-- Sempre scuro, come il widget che configura. -->
    <style name="WidgetConfigureTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/widget_scaffold</item>
        <item name="android:statusBarColor">@color/widget_scaffold</item>
        <item name="android:navigationBarColor">@color/widget_scaffold</item>
    </style>
</resources>
